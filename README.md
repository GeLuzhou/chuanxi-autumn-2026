# 一路向山 · 2026川西旅行地图

免费公开的 GitHub Pages 旅行网站：12 天行程、40 个地点、19 张有来源和许可说明的实景参考照片。

## 浏览和相册

- 在地图点选日期与景点，查看行程、景色特色和高德入口。
- 9 月 28 日晚回成都；10 月 4 日直接返回新都桥；10 月 5 日冷嘎措，不必等待日照金山。
- GitHub Pages 公开版不收集或存储用户上传的照片。「旅行相册」链接到原来的私人旅行手册，照片在那里上传、保存与管理，不会自动同步到公开网站。
- 地图连线仅示意游览顺序，不是驾车导航或徒步轨迹。

## 本地运行

需要 Node.js 22.13 或更高版本。

```sh
npm ci
npm run dev
```

根目录部署用 `npm run build`。GitHub Pages 项目子路径由自动发布流程设置 `PAGES_BASE_PATH`。

## 发布

仓库 Settings → Pages → Source 选择 GitHub Actions。每次推送到 main，工作流会检查类型、构建静态网页并发布。页面浏览不需要 GitHub 或 ChatGPT 登录。

仅 `dist` 构建目录成为网站内容；本项目不需要后端凭据、数据库或存储密钥。

## 资料与图片

行程和景点内容位于 `lib/trip-data.ts`，参考照片的来源、作者与许可位于 `lib/photo-credits.ts`，网站「关于」也能查看。照片根据其各自许可使用，不代表出行当日天气。贡嘎星轨照片仅为区域参考，并未确认具体湖名为冷嘎措。Leaflet 许可见 `vendor/leaflet.LICENSE`。
